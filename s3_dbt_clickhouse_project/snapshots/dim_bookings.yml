snapshots:
  - name: dim_bookings
    relation: ref('bookings')
    config:
      database: de_pet_project_db
      schema: gold
      strategy: timestamp
      unique_key: booking_id
      updated_at: created_at
      dbt_valid_to_current: "TO_DATE('9999-12-31', 'YYYY-MM-DD')"